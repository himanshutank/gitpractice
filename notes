checking out branch feature of git app.

checking out branch feature by implementing branch2 

"notes

git checkout master
git pull
git checkout <branch_name>
git pull
git rebase -i master
git checkout master
git merge
